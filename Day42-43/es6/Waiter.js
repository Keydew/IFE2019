class Waiter extends Employee {
    constructor(name, salary) {
        super(name, salary);
    }

    oneWork(course) {
        if (course instanceof Array) {
            //若是数组，则是记录点菜

        } else {
            //若不是，则是上菜

        }
    }
}